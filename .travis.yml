language: rust
rust: stable
cache: cargo

before_script:
    - export PATH="$PATH:$HOME/.cargo/bin"

before_install:
    - sudo apt-get update -qq
    - sudo apt-get install libgpgme-dev

script:
    - cargo test
    - cargo test --release
    - cargo doc --no-deps
